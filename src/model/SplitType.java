package model;

//An enum to identify the type of expense being added (EQUAL, EXACT, PERCENT).
//Makes your code type-safe and readable.
//Replaces error-prone string literals like "EQUAL".

public enum SplitType {
    EQUAL,
    EXACT,
    PERCENT
}

